import styled from "styled-components";
import { DefaultContainer, DefaultContent } from "../../styles/global";

export const HomeHighlihtsContainer = styled(DefaultContainer)`
    padding-top: 80px;

    h2 {
        font: var(--Heading-3-500);
        color: var(--theme-00);
    }
`

export const HomeHighlihtsContent = styled(DefaultContent)`
    height: 100vh;
`
